import React from 'react';

const BulkUpload = () => {
  return (
    <div className="p-6">
      <h2 className="text-3xl font-semibold">Bulk Upload</h2>
      <p>Upload product data in bulk using CSV or JSON files.</p>
      {/* Include file upload functionality and instructions */}
    </div>
  );
};

export default BulkUpload;
