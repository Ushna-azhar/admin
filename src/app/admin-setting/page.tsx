import React from 'react';

const AdminSettings = () => {
  return (
    <div className="p-6">
      <h2 className="text-3xl font-semibold">Admin Settings</h2>
      <p>Manage admin settings, including password, roles, and permissions.</p>
      {/* Add settings functionality, like changing password, managing roles, etc. */}
    </div>
  );
};

export default AdminSettings;
